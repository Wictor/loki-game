services:
  - type: web
    name: loki-server
    runtime: node
    region: frankfurt
    plan: free
    rootDir: .
    buildCommand: cd server && npm install
    startCommand: cd server && npx tsx src/index.ts
    envVars:
      - key: PORT
        value: 3001
